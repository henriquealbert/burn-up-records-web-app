query AllReleases($id: ID!) {
  user(id: $id) {
    id
    releases {
      id
      name
      date
      type
      status
      catalog
      created_at
      artwork {
        id
        url
        formats
      }
    }
  }
}
