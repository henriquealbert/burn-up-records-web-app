mutation createTrack($input: createTrackInput!) {
  createTrack(input: $input) {
    track {
      id
      name
      mix_name
      url
      release {
        id
        name
      }
      track_artist
      remixer_name
      genre
    }
  }
}
